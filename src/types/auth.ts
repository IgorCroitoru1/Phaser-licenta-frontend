export type SignInData = {
    email: string
    password: string
  }
  
  export type SignUpData = {
    name: string
    email: string
    password: string
  }
  
  export type Credentials = {
    access_token: string
  }